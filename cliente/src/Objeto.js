import React, { Component } from 'react';
import PropTypes from 'prop-types';
export default class Objeto extends Component {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

//declaramos el tipo del prop que le pasamos al componenete
Objeto.propTypes ={
  objeto: PropTypes
};
